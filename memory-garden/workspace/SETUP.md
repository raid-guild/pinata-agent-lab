# Setup

Status: pending

This file tracks whether the live Memory Garden instance has completed first-run setup.

During the first useful conversation, collect enough direction to adapt the memory workspace:

- user name or preferred handle
- topic clusters to track
- note types to capture
- resurfacing purpose
- cleanup and synthesis preference
- auth or OpenClaw proxy assumptions

When the direction is clear, update this file:

- Change `Status: pending` to `Status: complete`.
- Add a short memory garden setup summary.
- Add any immediate next steps.

Do not delete `BOOTSTRAP.md` automatically. `BOOTSTRAP.md` remains reusable first-run guidance; this file is the live instance setup marker.
