# Memory Garden

A Pinata-ready personal knowledge template where notes, links, ideas, and reflections grow into visual topic clusters over time.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000/app`.

## Validate

```bash
npm run typecheck
npm run build
```

The SQLite database is created at `data/memory-garden.sqlite` on first use.
